# GC 官網 — Netlify 設定
# 每次部署前先跑 SEO 預渲染：把 Supabase 的公開商品與 JSON-LD 灌進 catalog.html
# （若你已有 netlify.toml，請把下面 [build] 的 command 併進既有設定即可）

[build]
  command = "node build-seo.js"
  publish = "."          # ← 網站根目錄；若你的站台檔案不在 repo 根目錄，改成實際資料夾

[build.environment]
  NODE_VERSION = "20"
